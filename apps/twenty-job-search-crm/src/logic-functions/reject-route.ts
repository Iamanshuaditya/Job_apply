import{defineLogicFunction,type RoutePayload}from'twenty-sdk/define';import{Response}from'twenty-sdk/logic-function';import{IDS}from'src/constants/ids';import{orchestratorJson}from'./utils/orchestrator';import{refreshSnapshot}from'./utils/sync-crm';const handler=async(event:RoutePayload)=>{try{const body=(event.body as any)||{};await orchestratorJson('/api/jobs/reject',{method:'POST',body:{jobIds:body.jobIds??[]}});return new Response(JSON.stringify({ok:true,snapshot:await refreshSnapshot()}),{status:200,headers:{'Content-Type':'application/json'}})}catch(error){return new Response(JSON.stringify({error:error instanceof Error?error.message:String(error)}),{status:500})}};export default defineLogicFunction({universalIdentifier:IDS.reject_route,name:'job-search-reject',description:'Reject selected jobs.',timeoutSeconds:60,handler,httpRouteTriggerSettings:{path:'/job-search/reject',httpMethod:'POST',isAuthRequired:true}});
