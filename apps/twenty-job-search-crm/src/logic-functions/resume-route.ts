import{defineLogicFunction,type RoutePayload}from'twenty-sdk/define';import{Response}from'twenty-sdk/logic-function';import{IDS}from'src/constants/ids';import{orchestratorBytes}from'./utils/orchestrator';const handler=async(event:RoutePayload)=>{const jobId=event.queryStringParameters?.jobId;if(!jobId)return new Response('Missing jobId',{status:400});try{return new Response(await orchestratorBytes(`/api/resume/${encodeURIComponent(jobId)}`),{status:200,headers:{'Content-Type':'application/pdf','Content-Disposition':`inline; filename="resume-${jobId}.pdf"`}})}catch(error){return new Response(error instanceof Error?error.message:String(error),{status:404})}};export default defineLogicFunction({universalIdentifier:IDS.resume_route,name:'job-search-resume',description:'Streams the exact reviewed resume PDF.',timeoutSeconds:60,handler,httpRouteTriggerSettings:{path:'/job-search/resume',httpMethod:'GET',isAuthRequired:true}});
