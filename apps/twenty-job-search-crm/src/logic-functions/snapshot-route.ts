import{defineLogicFunction}from'twenty-sdk/define';import{Response}from'twenty-sdk/logic-function';import{IDS}from'src/constants/ids';import{refreshSnapshot}from'./utils/sync-crm';const handler=async()=>{try{return new Response(JSON.stringify(await refreshSnapshot()),{status:200,headers:{'Content-Type':'application/json'}})}catch(error){return new Response(JSON.stringify({error:error instanceof Error?error.message:String(error)}),{status:500})}};export default defineLogicFunction({universalIdentifier:IDS.snapshot_route,name:'job-search-snapshot',description:'Refresh job state from the control plane.',timeoutSeconds:60,handler,httpRouteTriggerSettings:{path:'/job-search/snapshot',httpMethod:'GET',isAuthRequired:true}});
