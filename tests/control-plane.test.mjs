import test from 'node:test';import assert from 'node:assert/strict';import os from 'node:os';import path from 'node:path';import{mkdtemp,writeFile,readFile}from'node:fs/promises';import{discoverGreenhouse,discoverLever,discoverAshby}from'../src/discovery/adapters.mjs';import{JobStore}from'../src/job-store.mjs';import{processJob}from'../src/pipeline.mjs';import{MockATS}from'../src/application/mock-ats.mjs';import{JobControlPlane}from'../src/control-plane.mjs';
const profile={version:'test-v1',identity:{name:'Test Candidate',email:'jobs@example.com',phone:'+910000000000',location:'India'},eligibility:{authorizations:['India']},education:[],evidence:[{id:'react',type:'experience-bullet',fact:'Built product experiences with React and TypeScript.',skills:['React','TypeScript'],verified:true},{id:'node',type:'experience-bullet',fact:'Built backend services using Node.js.',skills:['Node.js'],verified:true}]};const preferences={targetTitles:['Full Stack'],targetSkills:['React','Node.js','TypeScript'],excludedCompanies:[],workModes:['remote'],employmentTypes:['full-time'],thresholds:{manual:70,auto:80}};const job={company:'Acme Labs',title:'Full Stack Engineer',canonicalUrl:'https://jobs.example.test/acme/123',applicationUrl:'https://jobs.example.test/acme/123',externalId:'ACME-123',source:'fixture',description:'Build product experiences with React, TypeScript and Node.js.',locations:['Remote'],workMode:'remote',employmentType:'full-time',status:'active',requirements:[{requirement:'React production experience',kind:'must-have',skills:['React']},{requirement:'Node.js production experience',kind:'must-have',skills:['Node.js']},{requirement:'TypeScript experience',kind:'must-have',skills:['TypeScript']}]};const fakeResponse=(payload)=>({ok:true,json:async()=>payload});
test('public ATS discovery adapters normalize Greenhouse, Lever and Ashby',async()=>{const greenhouse=await discoverGreenhouse({kind:'greenhouse',company:'Acme',boardToken:'acme'},async()=>fakeResponse({jobs:[{id:1,title:'Full Stack Engineer',absolute_url:'https://boards.greenhouse.io/acme/jobs/1',content:'<p>React Node.js</p>',location:{name:'Remote'}}]}));assert.equal(greenhouse[0].company,'Acme');assert.match(greenhouse[0].description,/React Node\.js/);const lever=await discoverLever({kind:'lever',company:'Beta',site:'beta'},async()=>fakeResponse([{id:'l1',text:'Product Engineer',hostedUrl:'https://jobs.lever.co/beta/l1',applyUrl:'https://jobs.lever.co/beta/l1/apply',categories:{location:'Remote',commitment:'full-time'},descriptionPlain:'TypeScript'}]));assert.equal(lever[0].externalId,'l1');const ashby=await discoverAshby({kind:'ashby',company:'Gamma',boardName:'gamma'},async()=>fakeResponse({jobs:[{id:'a1',title:'Software Engineer',jobUrl:'https://jobs.ashbyhq.com/gamma/a1',applyUrl:'https://jobs.ashbyhq.com/gamma/a1/application',location:'Remote',descriptionHtml:'<b>React</b>',isListed:true}]}));assert.equal(ashby[0].company,'Gamma')});
test('job store remembers jobs and approval state without duplicate insertion',async()=>{const dir=await mkdtemp(path.join(os.tmpdir(),'job-store-'));const store=new JobStore(dir);const normalized={...job,jobId:'job-1',canonicalUrl:job.canonicalUrl,externalId:job.externalId};const first=await store.upsertDiscovered([normalized]);const second=await store.upsertDiscovered([normalized]);assert.equal(first.inserted,1);assert.equal(second.inserted,0);await store.patch('job-1',{crmStatus:'AWAITING_APPROVAL'});await store.approve(['job-1']);assert.equal((await store.get('job-1')).crmStatus,'APPROVED')});
test('prepare-only pipeline generates and verifies a resume but never submits',async()=>{const runDir=await mkdtemp(path.join(os.tmpdir(),'prepare-only-'));const ats=new MockATS('success');const result=await processJob({rawJob:job,rawProfile:profile,preferences,runDir,ats,submissionMode:'prepare-only'});assert.equal(result.state,'AWAITING_APPROVAL');assert.equal(result.verification.verdict,'pass');assert.equal(ats.received.length,0);assert.ok(result.resumePath);assert.ok((await readFile(result.resumePath)).length>0)});
test('control plane gates submission until explicit approval',async()=>{const dir=await mkdtemp(path.join(os.tmpdir(),'control-plane-'));const profileFile=path.join(dir,'profile.json');const preferencesFile=path.join(dir,'preferences.json');const sourcesFile=path.join(dir,'sources.json');await writeFile(profileFile,JSON.stringify(profile));await writeFile(preferencesFile,JSON.stringify(preferences));await writeFile(sourcesFile,JSON.stringify([{kind:'json-feed',company:'Acme Labs',url:'https://feed.test/jobs'}]));const ats=new MockATS('success');const fetchImpl=async()=>fakeResponse([job]);const control=new JobControlPlane({stateDir:path.join(dir,'state'),profileFile,preferencesFile,sourcesFile,ats,fetchImpl});const runAll=await control.runAll({prepareLimit:10});assert.equal(runAll.gate,'AWAITING_APPROVAL');assert.equal(ats.received.length,0);assert.equal((await control.jobs.list({statuses:['AWAITING_APPROVAL']})).length,1);await control.approve('all');const apply=await control.applyApproved({limit:10});assert.equal(apply.results[0].state,'SUBMITTED');assert.equal(ats.received.length,1);assert.equal((await control.snapshot()).summary.submitted,1)});
